import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { width: '100%', minHeight: 300, padding: 16, backgroundColor: '#343342', borderTopLeftRadius: 12, borderTopRightRadius: 12, position: 'absolute', bottom: 0, left: 0 },
    title: { fontWeight: 'bold', fontSize: 24, color: '#FFFFFF', marginVertical: 24 },
    closeButton: { position:'absolute', top: 16, right: 16, color: '#FFFFFF', fontSize: 24 },
    input: { width: '100%', height: 56, borderColor: '#FFFFFF', borderWidth: 1, borderRadius: 8, paddingLeft: 16, color: "#FFFFFF"},
    
})