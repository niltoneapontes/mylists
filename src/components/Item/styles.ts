import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { width: '100%', height: 52, backgroundColor: '#2B2A33', justifyContent: 'center', padding: 16, borderRadius: 8 },
    content: { fontWeight: 'regular', fontSize: 20, lineHeight: 20, color: '#FFFFFF' }
})