import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { width: '100%', height: 64, backgroundColor: '#2B2A33', padding: 16, borderRadius: 8, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center'},
    content: { fontWeight: 'regular', fontSize: 20, lineHeight: 20, color: '#FFFFFF' },
    iconsContainer: { flexDirection: 'row' },
    icon: { marginRight: 8 } 
})