import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { width: '100%', height: 56, backgroundColor: '#FBC02D', borderRadius: 8, alignItems: 'center', justifyContent: 'center', marginTop: 16},
    content: { fontWeight: 'bold', fontSize: 20, color: '#212029' }
})